const { Router } = require('express');

const MyController = require('./app/controllers/Mycontroller'); // Alter this line for you controller

const router = Router();

// ADD ROUTES

module.exports = router;
